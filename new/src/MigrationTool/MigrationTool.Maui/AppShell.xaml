<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MigrationTool.Maui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MigrationTool.Maui.Views"
    Shell.FlyoutBehavior="Flyout"
    FlyoutWidth="280"
    FlyoutBackgroundColor="{StaticResource Gray100}">

    <Shell.FlyoutHeader>
        <Grid HeightRequest="120" BackgroundColor="{StaticResource Primary}">
            <VerticalStackLayout VerticalOptions="Center" Padding="20,0">
                <Label Text="🔧" FontSize="32"/>
                <Label Text="Migration Tool" 
                       FontSize="22" 
                       FontAttributes="Bold" 
                       TextColor="White"/>
                <Label Text=".NET Project Migration" 
                       FontSize="12" 
                       TextColor="White" 
                       Opacity="0.8"/>
            </VerticalStackLayout>
        </Grid>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Dashboard">
        <FlyoutItem.Icon>
            <FontImageSource FontFamily="OpenSansRegular" Glyph="📊" Size="24"/>
        </FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:DashboardPage}" Route="dashboard"/>
    </FlyoutItem>
    
    <FlyoutItem Title="Project Explorer">
        <FlyoutItem.Icon>
            <FontImageSource FontFamily="OpenSansRegular" Glyph="📁" Size="24"/>
        </FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:ExplorerPage}" Route="explorer"/>
    </FlyoutItem>
    
    <FlyoutItem Title="Migration Planner">
        <FlyoutItem.Icon>
            <FontImageSource FontFamily="OpenSansRegular" Glyph="📋" Size="24"/>
        </FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:PlannerPage}" Route="planner"/>
    </FlyoutItem>
    
    <FlyoutItem Title="Settings">
        <FlyoutItem.Icon>
            <FontImageSource FontFamily="OpenSansRegular" Glyph="⚙️" Size="24"/>
        </FlyoutItem.Icon>
        <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" Route="settings"/>
    </FlyoutItem>

    <Shell.FlyoutFooter>
        <Grid HeightRequest="50" BackgroundColor="{StaticResource Gray200}" Padding="20,0">
            <Label Text="v1.0.0" 
                   VerticalOptions="Center" 
                   TextColor="{StaticResource Gray500}"
                   FontSize="12"/>
        </Grid>
    </Shell.FlyoutFooter>

</Shell>
