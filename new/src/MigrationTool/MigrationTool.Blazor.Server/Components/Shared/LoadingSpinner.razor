@inject ILocalizationService Localization

<div class="loading-container">
    <div class="spinner"></div>
    <p class="loading-text">@(Message ?? Localization.Get(Strings.Loading))</p>
</div>

@code {
    [Parameter] public string? Message { get; set; }
}
